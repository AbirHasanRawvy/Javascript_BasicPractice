console.log("Hello World!");

//Object




 //Operators

        //Arethremetic Operator;
        let a = 10;
        let b = 5;

        console.log(a + b);//additon;
        console.log(a - b);//substraction;
        console.log(a * b);//multification;
        console.log(a / b);//division;
        console.log(a % b);//modulus;
       
       //Assignment Operator;
       a = a + 5;
       console.log(a);
       a -= 2;
       console.log(a);
       b*=5;
       console.log(b);
       a /= 13;
       console.log(a);

       //Comparison Operator;
       
       //Logical Operator;

       //Unary and ternary Operator;
       
       //Unanry;
       a++;
       console.log(a);
       b--;
       console.log(b);
       
       //Ternary Operator;
       let boyos = 20;
       let isAdult = boyos > 18 ? "Adult" : "Minor";
       console.log(isAdult);
       let boyoss = 17;
       let isAdults = boyoss < 18 ? "Not Adult" : "Minor";
